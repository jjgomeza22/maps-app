import { MapView, MyLocationButton, ReactLogo } from "../components";

export const HomeScreen = () => {
  return (
    <div>
        <MapView />
        <MyLocationButton />
        <ReactLogo />
    </div>
  );
};
